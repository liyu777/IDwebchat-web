// import * as config from './config'
// import axios from 'axios'

export default {
  // 模糊查询用户用于添加好友
  serchUserForAdd (keywords) {
    // return axios.get(config.SETUP_HOST + 'userSettings/' + userId)
  }
}
